
/// @desc	Returns highest existing node index

/// @param	ignore_self*

var _index = undefined;
with(Node) if(id != other)
	if(index > _index || is_undefined(_index))
		_index = index;

if(is_undefined(_index)) return 0;
else return _index;