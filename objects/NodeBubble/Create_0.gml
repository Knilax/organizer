
// Creator
creator = noone;

// Set cursor position to end of body
alarm[2] = 1;

// Size
width = 172;
height = NODEBUBBLE_MARGIN_V;
arrow_size = width*0.075;

// Positions
arrow_x = x + width/2;
margin_h = NODEBUBBLE_MARGIN_H;
margin_v = NODEBUBBLE_MARGIN_V;

// Typing
backspace_holding_alarm = BACKSPACE_HOLDING_ALARM;
backspace_delete_alarm = BACKSPACE_DELETE_ALARM;

// Clear keyboard string
keyboard_string = "";

// Toggle typing pipe
alarm[0] = TYPING_PIPE_TOGGLE;

// Wait before size and position and stuff
visible = false;
image_alpha = 0;
alarm[1] = 1;